export declare type OutputVertex<T extends object> = {
    id: string;
    children: OutputVertex<T>[];
    _origin: T;
} & {
    [x in keyof T]: T[x];
};
export declare type OutputGraphVertex<V extends object, E extends object> = {
    _id: string;
    _relations: OutputGraphEdge<V, E>[];
} & {
    [x in keyof V]: V[x];
};
export declare type OutputGraphEdge<V extends object, E extends object> = {
    _u: OutputGraphVertex<V, E>;
    _v: OutputGraphVertex<V, E>;
    _origin: E;
} & {
    [x in keyof E]: E[x];
};
export declare class GraphConfig {
    isDirect: boolean;
}
export declare const defaultGraphConfig: GraphConfig;
