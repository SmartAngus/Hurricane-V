export interface BaseVertex {
    id: string;
}
export interface BaseEdge {
    u: string;
    v: string;
}
export declare type Vertex<T extends BaseVertex> = {
    [key in keyof T]: T[key];
};
export declare type Edge<T extends BaseEdge> = {
    [key in keyof T]: T[key];
};
export declare type VertexGroup<V extends BaseVertex, T extends BaseVertex> = {
    [key in keyof T]: T[key];
} & {
    vertexes: Vertex<V>[];
};
