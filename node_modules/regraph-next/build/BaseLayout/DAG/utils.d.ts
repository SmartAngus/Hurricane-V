import { InternalUpGradeNode } from './types';
declare function sortNodelevel<T extends {
    _median?: number;
    levelPos?: number;
    _levelPos?: number;
}>(nodelevel: T[]): T[];
declare function crossing<ON, OL>(upNodes: InternalUpGradeNode<ON, OL>[], downNodes: InternalUpGradeNode<ON, OL>[]): number;
declare function getRatio(idx: number, length: number): number;
export { getRatio, sortNodelevel, crossing, };
