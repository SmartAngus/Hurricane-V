import * as dagre from 'dagre';
import { Vertex, BaseVertex, Edge, BaseEdge, VertexGroup } from '../type';
export declare class BaseGroupLayout<N extends BaseVertex & {
    width: number;
    height: number;
}, NL extends BaseEdge, G extends BaseVertex & {
    expand: boolean;
    width?: number;
    height?: number;
}> {
    g: dagre.graphlib.Graph;
    nodes: Vertex<N>[];
    links: Edge<NL>[];
    groups: VertexGroup<N, G>[];
    getNodes(nodes: Array<Vertex<N>>): Vertex<N>[];
    getGroups(groups: Array<VertexGroup<N, G>>): VertexGroup<N, G>[];
}
