import { Matrix, LinkInfo } from './define';
import { BaseEdge } from '../../type';
declare class defaultProps {
    data: any;
    width: number;
    height: number;
    rowCount: number;
    minSize: number;
    maxSize: number;
}
export declare class Bubble<E extends BaseEdge> {
    private data;
    private matrix;
    private matrixRows;
    private matrixColumns;
    private heightInterval;
    private widthInterval;
    private rowCount;
    private columnCount;
    private maxSize;
    private minSize;
    private edges;
    constructor({ data, width, height, rowCount, minSize, maxSize }: defaultProps);
    private init;
    getNodes(): Matrix[];
    getLinks(): LinkInfo[];
    graph(): {
        nodes: Matrix[];
        links: LinkInfo[];
    };
}
export { Matrix, LinkInfo };
